version: "3.8"

services:
  jellysay:
    build:
      context: .
    ports:
      - "3535:3535"
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      TELEGRAM_CHAT_ID: ${TELEGRAM_CHAT_ID}
    volumes:
      - ./data:/app/data
      - ./log:/app/log